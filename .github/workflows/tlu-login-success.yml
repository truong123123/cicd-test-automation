name: 🟢 TLU Login - Test Đúng

on:
  push:
    branches: [ main, develop, 'feature/**', 'fix/**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  tlu-login-success:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          
      - name: Run Login Success Test
        run: mvn test -Dtest=TluLoginTest#testLoginSuccess
        env:
          CI: true
